name: Android Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.18.0'
      
      - name: Install dependencies
        run: npm install --legacy-peer-deps
      
      - name: Android Build Check
        run: |
          echo "Android build configuration check"
          # Android 빌드 관련 체크만 수행
          # 실제 빌드는 필요시 추가

