# DNS 설정 가이드 - www.runonapp.kr

GitHub Pages 커스텀 도메인을 위한 DNS 설정 방법입니다.

## 📋 설정 전 확인사항

1. 도메인 제공업체 확인 (예: 가비아, 후이즈, Route53, Cloudflare 등)
2. 도메인 관리자 페이지 접근 권한 확인
3. DNS 레코드 수정 권한 확인

---

## 🔧 DNS 설정 방법

### 방법 1: A 레코드 사용 (권장)

루트 도메인(`runonapp.kr`)과 서브도메인(`www.runonapp.kr`) 모두 사용하려면 A 레코드를 설정합니다.

#### A 레코드 설정

| 타입 | 호스트/이름 | 값/포인트 | TTL |
|------|------------|----------|-----|
| A | `@` | `185.199.108.153` | 3600 (또는 기본값) |
| A | `@` | `185.199.109.153` | 3600 (또는 기본값) |
| A | `@` | `185.199.110.153` | 3600 (또는 기본값) |
| A | `@` | `185.199.111.153` | 3600 (또는 기본값) |
| A | `www` | `185.199.108.153` | 3600 (또는 기본값) |
| A | `www` | `185.199.109.153` | 3600 (또는 기본값) |
| A | `www` | `185.199.110.153` | 3600 (또는 기본값) |
| A | `www` | `185.199.111.153` | 3600 (또는 기본값) |

**참고**: 
- `@`는 루트 도메인(`runonapp.kr`)을 의미합니다
- `www`는 서브도메인(`www.runonapp.kr`)을 의미합니다
- 4개의 IP 주소를 모두 추가해야 합니다 (로드 밸런싱)

---

### 방법 2: CNAME 레코드 사용

서브도메인(`www.runonapp.kr`)만 사용하려면 CNAME 레코드를 사용할 수 있습니다.

#### CNAME 레코드 설정

| 타입 | 호스트/이름 | 값/포인트 | TTL |
|------|------------|----------|-----|
| CNAME | `www` | `YOUR_GITHUB_USERNAME.github.io` | 3600 (또는 기본값) |

**예시**: 
- GitHub 사용자명이 `netgil-lee`인 경우: `netgil-lee.github.io`
- 저장소 이름이 `RunOn-App`인 경우: `netgil-lee.github.io`

**⚠️ 주의사항**:
- 루트 도메인(`runonapp.kr`)에는 CNAME을 사용할 수 없습니다
- 루트 도메인도 사용하려면 A 레코드를 사용해야 합니다

---

## 🏢 주요 도메인 제공업체별 설정 방법

### 1. 가비아 (Gabia)

1. [가비아 홈페이지](https://www.gabia.com) 로그인
2. **도메인 관리** → **DNS 관리** 메뉴 선택
3. 해당 도메인(`runonapp.kr`) 선택
4. **레코드 추가** 클릭
5. A 레코드 또는 CNAME 레코드 추가:
   - **호스트**: `@` 또는 `www`
   - **타입**: `A` 또는 `CNAME`
   - **값**: IP 주소 또는 GitHub Pages 주소
   - **TTL**: `3600` (또는 기본값)
6. **저장** 클릭

### 2. 후이즈 (Whois)

1. [후이즈 홈페이지](https://whois.co.kr) 로그인
2. **도메인 관리** → **DNS 관리** 메뉴 선택
3. 해당 도메인 선택
4. **레코드 추가** 클릭
5. A 레코드 또는 CNAME 레코드 추가
6. **적용** 클릭

### 3. AWS Route 53

1. AWS 콘솔 로그인
2. **Route 53** → **호스팅 영역** 선택
3. `runonapp.kr` 호스팅 영역 선택
4. **레코드 생성** 클릭
5. 레코드 설정:
   - **레코드 이름**: `@` 또는 `www`
   - **레코드 유형**: `A - IPv4 주소` 또는 `CNAME`
   - **값**: IP 주소 또는 GitHub Pages 주소
   - **TTL**: `300` (5분)
6. **레코드 생성** 클릭

### 4. Cloudflare

1. [Cloudflare 대시보드](https://dash.cloudflare.com) 로그인
2. 도메인 선택
3. **DNS** 탭 클릭
4. **레코드 추가** 클릭
5. 레코드 설정:
   - **타입**: `A` 또는 `CNAME`
   - **이름**: `@` 또는 `www`
   - **IPv4 주소** 또는 **대상**: IP 주소 또는 GitHub Pages 주소
   - **프록시 상태**: `DNS 전용` (회색 구름) - GitHub Pages는 프록시를 사용하지 않습니다
   - **TTL**: `Auto` 또는 `300`
6. **저장** 클릭

### 5. 네임칩 (Namecheap)

1. [Namecheap 로그인](https://www.namecheap.com)
2. **Domain List** → 해당 도메인 선택
3. **Advanced DNS** 탭 클릭
4. **Add New Record** 클릭
5. 레코드 추가:
   - **Type**: `A Record` 또는 `CNAME Record`
   - **Host**: `@` 또는 `www`
   - **Value**: IP 주소 또는 GitHub Pages 주소
   - **TTL**: `Automatic` 또는 `30 min`
6. **Save All Changes** 클릭

---

## ⏱️ DNS 전파 시간

- **일반적인 전파 시간**: 1-24시간
- **최대 전파 시간**: 최대 48시간
- **빠른 확인 방법**: 
  - [whatsmydns.net](https://www.whatsmydns.net)에서 전파 상태 확인
  - `www.runonapp.kr` 검색

---

## ✅ 설정 확인 방법

### 1. DNS 레코드 확인

터미널에서 확인:
```bash
# A 레코드 확인
dig www.runonapp.kr +short

# CNAME 레코드 확인
dig www.runonapp.kr CNAME +short

# 모든 레코드 확인
nslookup www.runonapp.kr
```

온라인 도구:
- [whatsmydns.net](https://www.whatsmydns.net)
- [dnschecker.org](https://dnschecker.org)

### 2. GitHub Pages 설정 확인

1. GitHub 저장소 → **Settings** → **Pages**
2. **Custom domain**에 `www.runonapp.kr` 입력
3. **Save** 클릭
4. DNS 설정이 올바르면 체크 표시(✓)가 나타납니다
5. **Enforce HTTPS** 옵션이 활성화되면 체크 (DNS 전파 후 몇 시간 소요)

---

## 🔍 문제 해결

### 문제 1: DNS 전파가 안 됨

**해결 방법**:
- 24-48시간 대기
- DNS 캐시 클리어:
  ```bash
  # macOS/Linux
  sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder
  
  # Windows
  ipconfig /flushdns
  ```

### 문제 2: GitHub Pages에서 도메인 인증 실패

**해결 방법**:
- DNS 레코드가 올바르게 설정되었는지 확인
- TTL 값을 낮춰서 빠른 전파 확인 (300초 권장)
- GitHub Pages 설정에서 도메인을 삭제하고 다시 추가

### 문제 3: HTTPS 인증서 발급 실패

**해결 방법**:
- DNS 전파 완료 후 24시간 대기
- GitHub Pages 설정에서 **Enforce HTTPS** 옵션 확인
- 도메인을 삭제하고 다시 추가

### 문제 4: www와 루트 도메인 모두 사용하고 싶음

**해결 방법**:
- 루트 도메인(`runonapp.kr`): A 레코드 4개 추가
- 서브도메인(`www.runonapp.kr`): A 레코드 4개 또는 CNAME 1개 추가
- GitHub Pages 설정에서 두 도메인 모두 추가 가능

---

## 📝 체크리스트

- [ ] DNS 레코드 추가 완료 (A 레코드 4개 또는 CNAME 레코드)
- [ ] DNS 전파 확인 (whatsmydns.net 사용)
- [ ] GitHub 저장소 Settings > Pages에서 Custom domain 설정
- [ ] GitHub Pages에서 도메인 인증 완료 (체크 표시 확인)
- [ ] HTTPS 인증서 발급 완료 (24시간 후)
- [ ] `https://www.runonapp.kr` 접속 테스트
- [ ] `https://www.runonapp.kr/privacy-policy.html` 접속 테스트

---

## 📞 추가 도움말

- [GitHub Pages 커스텀 도메인 공식 문서](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)
- [GitHub Pages IP 주소](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site/managing-a-custom-domain-for-your-github-pages-site#configuring-a-subdomain)

---

**작성일**: 2026년 1월 28일  
**도메인**: www.runonapp.kr  
**GitHub Pages IP**: 185.199.108.153, 185.199.109.153, 185.199.110.153, 185.199.111.153
